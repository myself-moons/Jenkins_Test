pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                echo 'Cloning repository'
                git 'https://github.com/myself-moons/Jenkins_Test'
            }
        }
    }
}